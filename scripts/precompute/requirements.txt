# precompute 의존성 (KICKOFF §8.3 — Python 3.11+, 재성 로컬 RTX 3080 실행)
#
# 반드시 전용 가상환경(.venv-precompute)에 설치할 것 — 기본(Anaconda) 환경에
# 설치하면 기존 도구들의 numpy/protobuf 의존성이 깨진다(2026-08-06 실제 발생).
#   python -m venv .venv-precompute
#   .venv-precompute/Scripts/pip install -r scripts/precompute/requirements.txt
#
# CUDA torch는 https://pytorch.org 안내대로 설치 권장 (next_token.py·
# attention_trace.py). CPU 휠(--index-url https://download.pytorch.org/whl/cpu)
# 로도 동작한다 — 1.3b 모델의 짧은 문장 트레이스는 CPU로 충분(2026-08-07 확인).
# embedding_map.py만 쓸 경우 numpy·umap-learn만 있으면 된다.
torch>=2.3
transformers>=4.56
numpy>=2
umap-learn>=0.5
